# This module currently has no outputs defined.
# All necessary information is available through kubectl commands once deployed.
#
# Example commands:
# - kubectl get ingress -n keycloak-prod
# - kubectl get managedcertificate -n keycloak-prod
# - kubectl get pods -n keycloak-prod
